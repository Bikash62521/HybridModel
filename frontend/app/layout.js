import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "NeuroSentry — EEG State Classifier",
  description:
    "Upload EEG data and classify brain states using a hybrid CNN + SVM deep-learning model. Detect seizures, tumors, and healthy brain activity instantly.",
  keywords: ["EEG", "seizure detection", "CNN", "SVM", "brain", "neuroscience"],
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
